<h3>Write a comment</h3>
<%= form_for [@post, @comment] do |f| %>

  <p>
    <%= f.text_area :content, rows: "3", placeholder: "Comment", class: "textarea form-control"%>
  </p>

  <p>
    <%= f.submit 'Submit', class: "btn pull-right"%>
  </p>
  <% end %>

<h1>Blog</h1>

<ul>
  <% @posts.each do |post| %>

  <li>
   <h2> <%=link_to post.title, post_path(post), class: 'post_title', id: "post_#{post.id}" %></h2>
   <p> <%= post.body %> </p>
  </li>
  <% end %>
</ul>

<%= link_to "Create a new post", new_post_path, class: "new_post" %>
